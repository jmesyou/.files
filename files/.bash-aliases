# .bash-aliases

alias emacs='emacs -nw';
alias please='sudo';

if type exa &> /dev/null ; then 
    alias ls='exa';
fi 

if [[ "$TERM" == "xterm-kitty" ]] ; then   
    alias ssh='kitty +kitten ssh';
fi
